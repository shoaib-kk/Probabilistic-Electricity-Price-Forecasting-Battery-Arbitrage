# Minimal dependencies for the scheduled live tick (scripts/live_tick.py).
# No torch/stable-baselines3: live inference uses exported numpy policies.
# lightgbm/scikit-learn/joblib are pinned to the versions that pickled
# models/deployed/forecaster.joblib.
pandas==2.3.3
numpy==2.4.1
requests==2.32.5
lightgbm==4.6.0
scikit-learn==1.8.0
joblib==1.5.3
gymnasium==1.3.0
